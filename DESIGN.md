# 宅序移动端 UI 设计说明

## 视觉定位

现代东方空间咨询工具：有文化气质，但不使用卷轴、木纹、龙纹、八卦图或大面积金色。平面图始终是主角，方案文字只解释当前点位。

## 设计系统

- 背景：暖纸白 `#F7F4ED`
- 主文字：墨黑 `#252927`
- 主品牌及已确认：古铜土黄 `#86602A`（深一档 `#6B4C1F` 用于按压/加深场景）
- 方位辅助线与选中高亮：赭金 `#B48746`；赭金用于文字时同取 `#86602A`，保证对比度不低于 4.5:1
- 辅助文字：暖灰 `#63665D`（在纸白底上约 5.3:1）
- 登录页背景：土黄径向渐变 `#A3824C → #82632F → #5C451E`
- 全系统禁用绿色系，品牌色统一为土黄/古铜金体系
- 待确认警示：朱砂 `#AD4035`
- 标题：宋体类字体，正文和控件：系统无衬线字体
- 组件：细描边、轻阴影、12–18px 功能性圆角
- 图标：1.6–1.9px 圆角线性图标

## 主界面结构

1. 安静的项目标题栏。
2. 平面图与施工清单双视图。
3. 可缩放、平移、点选的户型画布。
4. 编号点位与底部方案抽屉双向联动。
5. 展开方案后显示实施说明、状态和确认操作。

## Web 端结构

1. 底部为首页、项目、商城、咨询、我的导航（与 App 端一致，均面向客户；分析与咨询由 AI 完成，无老师端）。
2. 项目页中间为可缩放、可点选的平面图工作画布。
3. 项目页右侧为当前点位的布局建议、实施节点和待确认内容。
4. 首页、商城、咨询、我的复用 App 端页面组件，桌面端限宽排版。
5. 客户点位数据由 App 与 Web 共用，两端采用相同编号、颜色和状态语言。

## AI 户型分析

1. 首页提供户型图上传入口，AI（Claude 视觉模型）识别格局并输出 3-6 个问题点位。
2. 点位含方位、空间、方案简称、建议、施工时机、待确认事项与图上百分比坐标。
3. 每次分析生成一个新项目，保存进「项目」列表（localStorage），不覆盖旧方案。
4. 「项目」页是方案存放处：列表展示全部方案，点击进入对应的画布工作台；施工清单按实施时机自动分组。

## 移动端约束

- 以 `390 × 844` 为主要验收尺寸。
- 正文不小于 14px，辅助说明不小于 12px，主要操作不小于 44px；底部导航标签按平台惯例允许 11px。
- 底部方案抽屉默认只占约 250px，可展开；不能永久遮住大半户型图。
- 颜色不能作为唯一状态表达，必须同时提供文字或图标。

## 响应式边界

- 小于 `1100px` 使用 App 端结构。
- 大于等于 `1100px` 使用 Web 端三栏工作台。
- Web 户型画布同时受可用宽度和可视高度约束，避免在 `1280 × 720` 下裁切建筑底部。
